<!-- views/pages/attack.ejs -->

<!DOCTYPE html>
<html lang="en">
<head>
    <% include ../partials/head %>
</head>
<body class="container anarchist">

  <div class="row spaced">
    <div class="u-full-width center-text">
      <h1 class="title">
        <span>THE</span>
        <img src="/images/anarchist_star.png">
        <span class="right">GITATOR</span>
      </h1>
    </div>
  </div>

  <div class="row">
    <div class="six columns">
      <span><%- date %></span>
    </div>
    <div class="six columns">
      <span class="u-pull-right">16 Years of Anarchist News for the People</span>
      <span class="u-cf"></span>
    </div>
  </div>

  <div class="row spaced">
    <div class="u-full-width center-text">
      <% if(article == 'attack') { %>
        <% include ../partials/agitator/attack %>
      <% } else if(article == 'harbor') { %>
        <% include ../partials/agitator/harbor %>
      <% } %>

    </div>
  </div>

  <% include ../partials/footer %>

  <% if(convo != null) { %>
  <script src="https://code.jquery.com/jquery-3.1.1.min.js" crossorigin="anonymous"></script>
  <script src="/vendor/html2canvas.js"></script>
  <script src="/vendor/glitch.js"></script>

  <script>
    $(document).ready(function() {
      var convo = <%- JSON.stringify(convo) %>;
      if(convo == false) {
        setTimeout(function() {
          $('body').glitch('replace', {
            amount: 10
          });
          openChatDelay();
          setTimeout(function() {
            //ajax post, reroute without chat var
            var url = '/agitate?c='+null+'a='+<%- JSON.stringify(article) %>+'d='+<%- JSON.stringify(convo) %>;
            window.location.replace(urlString);
            /*$.ajax({
              type: 'POST',
              data: JSON.stringify(data),
              contentType: 'application/json',
              url: '/agitator',            
              success: function(data) {
                console.log('success');
              }
            });*/
            //location.reload();
          }, 5000);
        }, 1500);
      }
    });
  </script>
  <% } %>
</body>
</html>
